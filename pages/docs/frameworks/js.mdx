import { Callout } from "nextra/components";

Looking for the best JavaScript cheats and snippets? SheCodes Javascript Cheatsheet has you covered! We offer a wide range of JavaScript cheat sheets, including fundamentals and methods. Our cheat sheets are designed to help you quickly reference the most commonly used JS techniques for creating dynamic web pages. Whether you're new to JavaScript or an experienced developer, our cheat sheets and code examples are a valuable resource.

## General

### Comments

```js
// this is a comment
/* or this is a comment */
```

<Callout type='default'>
  This code will be ignored. Comments are generally a bad idea, your code should
  be explicit enough as it is.
</Callout>

## Variables

### Variable creation

```js
let school = "SheCodes";
let fullPackage = "SheCodes Pro";
let projects = 4;
let awesome = true;
```

### Variable operations

```js
let x = 2;
let y = 3;
let z = x + y; // 5
let city = "Brasilia";
let country = "Brasil";
let place = city + " " + country; //Brasilia Brasil
```

### Variable data Types

```js
let age = 25; // Number
let name = "Angelo"; // String
let canCode = true; // Boolean, could also be false
```

### Structure structure types

```js
let students = ["Kate", "Julie", "Mariana"]; // Array
let kate = {
  firstName: "Kate",
  lastName: "Johnson",
  age: 23,
  canCode: true,
}; // Object
```

## Alerts & Prompts

### Alert

```js
alert("Olá");
let name = "Angela";
alert(name);
```

### Prompt

```js
let firstName = prompt("What is your first name");
let lastName = prompt("What is your last name");
let fullName = firstName + " " + lastName;
alert(fullName);
```

## If else

### if statement

```js
let country = prompt("What country are you from?");
if (country === "Portugal") {
  alert("You are cool");
}
if (country !== "Portugal") {
  alert("Too bad for you");
}
```

### if else statement

```js
let age = prompt("How old are you?");
if (age < 18) {
  alert("You cannot apply");
} else {
  alert("You can apply");
}
```

### Nested if else statements

```js
if (age < 18) {
  alert("you can't apply");
} else {
  if (age > 120) {
    alert("you can't apply");
  } else {
    alert("you can apply");
  }
}
```

### Logical Or

```js
if (age < 18 || gender === "male") {
  alert("You can't join SheCodes 👩‍💻");
}
```

<Callout type='default'>
  The code will be executed if one statement is true.
</Callout>

### Logical And

```js
if (continent === "Europe" && language === "Portuguese") {
  alert("You are from Portugal 🇵🇹");
} else {
  alert("You are not from Portugal");
}
```

<Callout type='default'>
  The code will be executed if both statements are true.
</Callout>

### Comparison and Logical Operators

```js
2 > 3; // false
2 < 3; // true
2 <= 2; // true
3 >= 2; // true
2 === 5; // false
2 !== 3; // true
1 + 2 === 4; // false
```

## Strings

### Creating a string

```js
let name = "usbaliendev"; // "usbaliendev"
```

### String concatenation

```js
let firstName = "Angelo";
let lastName = "S Dorfey";
let fullName = firstName + " " + lastName; // "Angelo S Dorfey"
//or
let fullName = `${firstName} ${lastName}`;
```

### Trim

```js
let city = "Brasilia";
city.trim(); // "Brasilia"
```

### Replace

```js
let city = "Brasilia";
city = city.replace("i", "í"); // "Brasília"
```

### toLowerCase

```js
let city = "Brasília";
city = city.toLowerCase(); // "brasília"
```

### toUpperCase

```js
let city = "Brasília";
city = city.toUpperCase(); // "BRASILIA"
```

### Template literals

```js
let city = "Brasília";
let sentence = `Angelo is from ${city}`; // Kate is from Brasília
```

## Arrays

### Array declaration

```js
let myList = [];
let fruits = ["apples", "oranges", "bananas"];
myList = [
  "banana",
  3,
  go,
  ["John", "Doe"],
  { firstName: "John", lastName: "Smith" },
];
```

### Access an Array

```js
fruits;
fruits[0];
fruits[1];
fruits[2];
fruits[3];
```

### Update an Array item

```js
fruits[1] = "Mango";
fruits[1] = 3;
```

### while loop

```js
let times = 0;
while (times < 10) {
  console.log(times);
  times = times + 1;
}
```

### forEach loop

```js
let fruits = ["apples", "oranges", "bananas"];
fruits.forEach(function (fruit) {
  alert("I have " + fruit + " in my shopping bag");
});
```

### do while loop

```js
let times = 0;
do {
  console.log(times);
  times = times + 1;
} while (times < 10);
```

### for loop

```js
for (let i = 0; i < 10; i++) {
  console.log("i is " + i);
}
for (let i = 0; i < myList.length; i++) {
  alert("I have " + myList[i] + " in my shopping bag");
}
```

### Remove first item

```js
fruits.shift();
```

## Dates

### Get current time

```js
let now = new Date();
```

### Create a date

```js
let date = Date.parse("01 Jan 2025 00:00:00 GMT");
```

### Get date data

```js
let now = new Date();
now.getMinutes(); // 0,1,2, 12
now.getHours(); //1, 2, 3, 4
now.getDate(); //1, 2, 3, 4
now.getDay(); // 0, 1, 2
now.getMonth(); // 0, 1, 2
now.getFullYear(); // 2021
```

## Numbers

### Round

```js
Math.round(4.7); // 5
```

### Floor

```js
Math.floor(4.7); // 4
```

### Ceil

```js
Math.ceil(4.7); // 5
```

### Min

```js
Math.min(2, 5, 1); // 1
```

### Max

```js
Math.max(2, 5, 1); // 5
```

### Random

```js
Math.random(); // 0.47231881595639025
```

## Objects

### Creating a new object

```js
let fruit = new Object(); // "object constructor" syntax
let user = {}; // "object literal" syntax
let student = {
  firsName: "Angelo",
  lastName: "S Dorfey",
};
let anotherStudent = {
  firstName: "Kate",
  lastName: "Robinson",
  female: true,
  greet: function () {
    alert("Hey");
  },
};
```

### Reading an object properties

```js
let user = {
  firstName: "Ryan",
  lastName: "Reinolds",
  gender: "male",
};
alert(user.firstName); // Ryan
alert(user.lastName); // Reinolds
// or
alert(user["firstName"]); // Ryan
alert(user["lastName"]); // Reinolds
```

### Adding object properties

```js
let user = {
  firstName: "Ryan",
  lastName: "Reinolds",
  gender: "male",
};
user.profession = "Actor";
```

### Object Arrays

```js
let users = [
  {
    firstName: "Bradley",
    lastName: "Cooper",
  },
  {
    firstName: "Will",
    lastName: "Smith",
  },
];
users.forEach(function (user, index) {
  for (let prop in user) {
    alert(prop + " is " + user[prop]);
  }
});
```

### Enumerating the properties of an object

```js
let user = {
  firstName: "Angelo",
  lastName: "S Dorfey",
  gender: "male",
};
for (let prop in user) {
  alert(prop); // firstName, lastName, gender
  alert(user[prop]); // 'Angelo', 'S Dorfey', 'male'
}
```

## Functions

### JS Functions

```js
function sayFact() {
  let name = prompt("What's your name?");

  if (name === "Angelo") {
    alert("Your name comes from the Greek -> Angelus, “messenger of God” ");
  }
}
sayFact();
```

<Callout type='default'>Piece of code that does one or more actions</Callout>

### JS Functions Parameters

```js
function fullName(firstName, lastName) {
  alert(firstName + " " + lastName);
}
let firstName = prompt("What's your first name?");
let lastName = prompt("What's your last name?");
fullName(firstName, lastName);
fullName("Angelo", "S Dorfey");
```

### JS Functions Return

```js
function add(x, y) {
  return x + y;
}
let result = add(3, 4);
let result2 = add(result, 0);
function getFullName(firstName, lastName) {
  let fullName = firstName + " " + lastName;
  return fullName;
}
let userFullName = getFullName("Angelo", "S Dorfey");
alert(userFullName); // Angelo S Dorfey
alert(getFullName("UsbAlien", "Dev")); // UsbAlien Dev
```

### Closures

```js
function hello() {
  function go(name) {
    alert(name);
  }
  let name = "SheCodes";
  go(name);
}
hello();
```

## Debugging

### Console.log

```js
console.log(name);
console.log("Let's code!");
```

<Callout type='default'>Outputs a message to the web console.</Callout>

## Selectors

### QuerySelector

```js
let li = document.querySelector("li");
let day = document.querySelector(".day");
let paragraph = document.querySelector("ul#list p");
```

<Callout type='default'>
  Returns the first element (if any) on the page matching the selector.
</Callout>

### QuerySelectorAll

```js
let lis = document.querySelectorAll("li");
let paragraphs = document.querySelectorAll("li#special p");
```

<Callout type='default'>
  Returns all elements (if any) on the page matching the selector.
</Callout>

## Events

### Creating an event listener

```js
function sayHi() {
  alert("hi");
}
let element = document.querySelector("#city");
element.addEventListener("click", sayHi);
```

<Callout type='default'>
  It will only alert Hello after a 3 second delay
</Callout>

### setTimeout

```js
function sayHello() {
  alert("Hello");
}
setTimeout(sayHello, 3000);
```

<Callout type='default'>It will say Hello every 3 seconds</Callout>

### setInterval

```js
function sayHello() {
  alert("Hello");
}
setInterval(sayHello, 3000);
```

<Callout type='default'>It will say Hello every 3 seconds</Callout>

## AJAX

### AJAX with Fetch

```js
let root = "https://jsonplaceholder.typicode.com";
let path = "users/1";
fetch(root + "/" + path)
  .then((response) => response.json())
  .then((json) => console.log(json));
```

<Callout type='default'>Note: We recommend axios instead</Callout>

### AJAX with Axios

```js
<!DOCTYPE html>
<html>
  <head>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
  </head>
  <body>
    <script>
      function showUser(response) {
        alert(`The user name is ${response.data.name}`);
      }
      let url = "https://jsonplaceholder.typicode.com/users/1";
      axios.get(url).then(showUser);
    </script>
  </body>
</html>
```

## Element Manipulation

### HTML classes

```js
let li = document.querySelector("li#special");
li.classList.remove("liked");
li.classList.add("something");
```

<Callout type='default'>Update the element class names.</Callout>

### HTML content

```js
let li = document.querySelector("li");
li.innerHTML = "Hello World";
```

<Callout type='default'>
  Update the HTML content of the selected element.
</Callout>

### Forms

```js
<form>
  <input type="text" id="email" />
</form>
<script>

function signUp(event) {
  event.preventDefault();
  let input = document.querySelector("#email");
  console.log(input.value);
}
let form = document.querySelector("form");
form.addEventListener("submit", signUp);
</script>
```

<Callout type='default'>
  Note: The event will be triggered by clicking the button or pressing enter.
</Callout>

## APIs

### Geolocation API

```js
function handlePosition(position) {
  console.log(position.coords.latitude);
  console.log(position.coords.longitude);
}

navigator.geolocation.getCurrentPosition(handlePosition);
```

<Callout type='default'>
  The Geolocation API allows the user to provide their location to web
  applications if they so desire. For privacy reasons, the user is asked for
  permission to report location information.
</Callout>
